// I need to:
// 1. Give the map far more density. Increase prop spawn rate, add new prop types. 
// 2. Add different car models. Currently it builds one interceptor. There's a `state.equipment.chassis.name`. I will read the chassis name in `createCar()` or `rebuildCarAppearance()` to swap out the entire mesh!
// 3. Make the map much larger: increase road width? Or increase prop bounds and density.
